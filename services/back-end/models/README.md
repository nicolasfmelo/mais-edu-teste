# Whisper Models

This folder is reserved for local Whisper model assets used by the backend audio route.

Expected default path at runtime:

- `services/back-end/models/whisper-small`

You can override this location with `WHISPER_MODEL_PATH`.
